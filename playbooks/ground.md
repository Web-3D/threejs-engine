---
domain: ground
title: Ground — sân nền đa-tầng (zones) + khoét (cut) lộ lớp dưới
status: building
tier: —
modules:
  - threejs-modules/site/render/fromState.ts
  - threejs-modules/site/state.ts
  - threejs-modules/site/terrain.ts
issues:
  - KI-011
updated: 2026-06-07
---

# Playbook — Ground (sân nền đa-tầng + khoét)

> **Ranh giới:** recipe + sơ đồ tầng/toạ độ + lịch sử. Lỗi chi tiết → `known-issues/KI-NNN`. API/props → module README.

## 1. Kết quả "hoàn chỉnh"

Base ground (G0) = slab liền dưới đáy. Trên đó xếp **G-level** (1,2,…); mỗi level nhiều **zone** (mảng material
rect/circle/ellipse) đồng phẳng. **Cut (khoét)** cùng level đục lỗ CHÍNH XÁC mọi shape (phủ một phần/cắt mép
cong/xóa hết) → **lộ lớp ngay dưới** (zone level thấp hơn hoặc base), KHÔNG viền/góc thừa/wedge.

## 2. Recipe dựng

- **State** (`state.ts`): `GroundLayer` = `material` + `thickness` + `shape`(rect/circle/ellipse/free) + `points` +
  `offsetX/Z` + **`level`**(1-based) + **`op`**(add\|cut). `groundLayers[]` PHẲNG (flat) — `level`/`op` nhóm lúc render.
  **`site.groundLevels`** (số, optional) = SỐ G-level tường minh → cho phép tầng RỖNG; parse migrate `?? max(level)`,
  luôn ≥ max(level) (đừng ẩn tầng có layer). Render vẫn DERIVE level từ layers (rỗng = no-op, Y-stacking +0).
- **Render** (`fromState.ts` → `buildGroundLayers`): mỗi level → `buildLevelZones` (zones đồng phẳng @baseY) rồi
  `buildLevelCutPatches` (mảng xám highlight, ẩn).
- **Khoét = polygon-boolean** (`layerGeometry`): `difference([contour], ...cuts+nước)` (lib `polygon-clipping`) →
  MultiPolygon → mảng `Shape` → `ExtrudeGeometry`. Rỗng → `null` → bỏ mesh. KHÔNG earcut-hole (→ KI-011).
- **🕳️ Khoét lỗ HỒ — THỐNG NHẤT quanh 1 nguồn `pondWorldXZ`** (tessellate water-shape MỌI form ở `shapes.ts`; đổi
  shape hồ → lan hết). 2 biến thể có-chủ-đích + cơ chế theo từng vùng:

  | Vùng khoét | Polygon | Vì sao khác | Cơ chế |
  | --- | --- | --- | --- |
  | **Base ground** (phẳng `lotShape` · gò `griddedGroundGeometry` · editor backdrop `_rebuildEditorGround` · grid `_rebuildGrid`) | **`waterPolygons`** = `pondWorldXZ` (water-only, KHÔNG edge, KHÔNG puddle) | base NẰM DƯỚI coping → coping đặt LÊN base (base che dưới coping) | phẳng/editor = earcut `Shape.holes`; gò = **clip ô-biên `polygon-clipping`** (commit) / bỏ-ô (live) |
  | **Zone/layer chồng** (`layerGeometry`) | **`allWaterCarvePolygons`** = `pondWorldXZ` **+ `offsetPolygon(edgeWidth)`** + puddle | tầng TRÊN không che coping → khoét RỘNG hơn (lộ coping) | `polygon-clipping` difference (gồm cả vùng cut) |

  Nguyên tắc: **base = water-only (che-dưới-coping)**; **tầng-trên = water+edge (né-coping)**. Puddle (phẳng, không
  basin) → base KHÔNG khoét (puddle nằm trên nền); zone-chồng CÓ né. Coping `buildPoolEdge` = vành `pondWorldXZ →
  offsetPolygon(edgeWidth)`. ⚠ Đừng đổi base sang `allWaterCarvePolygons` (thủng dưới coping + puddle lơ lửng — đã vấp).
- **Editor** (archplan `gui/site.ts`): nested-tabs `G0(base)|G1|G2…|＋`; mỗi G = `[Mảng add | Khoét cut]`, mỗi tab
  = instance `Z1|Z2|＋` / `C1|C2|＋`. Cut highlight xám hiện CHỈ khi layer active (chọn tab GUI / click 3D / Move-drag)
  qua `ctx.setActiveGroundLayer` → Lab toggle `.visible` (raycaster vẫn pick dù ẩn). Tab G enumerate `1..groundLevels`
  (`groundEditorLevels`) → **`＋` G = +1 tầng RỖNG** (KHÔNG tự sinh Z1); **`✕ Remove G`** = xoá tầng + dồn level trên xuống.
- **Terrain (nền lồi-lõm, Phase 1)** (`terrain.ts` + `buildGround`): `site.terrain` (optional) bật → base ground =
  LƯỚI `resolution²` displaced Y bằng `heightAt(hf,x,z)` = **mask · (FBM noise nền + Σ gò)** ÷1000. **mask=0** ở
  pad nhà (`opts.buildingFootprint` = `_foundationRects`) ∪ quanh hồ (waterRect+edge) ∪ viền lô (`edgeFlat` band) →
  smoothstep →1 ở garden ⇒ **móng/hồ/rào/lot-edge GIỮ PHẲNG, không đổi**. Đục lỗ hồ = `waterPolygons` (KHỚP path
  phẳng — xem bảng "Khoét lỗ hồ" dưới): commit → **CLIP ô-biên `polygon-clipping`** (mép sạch như cut C1); live-drag
  → bỏ-ô nhanh (răng cưa tạm, né clip Martinez mỗi frame, `groundGeometry(.., clean=false)`). terrain tắt → path
  phẳng cũ. **Cỏ bám gò = Phase 2 ✅** (`heightAt` closure vào GrassBlades — gốc lá + vệt tiếp đất nâng theo gò;
  height-field DÙNG CHUNG maskRects với nền vì `exclude` cỏ ≡ `buildHeightField` → cỏ ngồi khít; xem §5). Slider noise đầy đủ ở GUI (`buildTerrainControls`).
  **Gò nặn tay = Phase 3a ✅** (`buildMoundControls`: list + `＋Thêm gò`/`✕Xoá` + 5 slider Pos X/Z · Radius · Height · Falloff → ghi `terrain.mounds[]`, cộng vào `heightAt` qua `moundsHeightMm` Σ-gò có sẵn từ P1). `＋` tự bật terrain. height ÂM (lõm) bị `Math.max(dy,0)` chặn (chỉ cancel noise, không thủng base). **Nặn 3D = Phase 3b ✅** (`interaction/moundDrag.ts` MoundTool, mirror GroundTool): Move mode + terrain bật → mỗi gò hiện 2 handle — TÂM (vàng, kéo XZ → dời x/z) + BÁN KÍNH (cyan mép, kéo → radius); kéo=`applyTerrainLive`, buông=commit, đĩa footprint lúc kéo. Height/Falloff giữ slider (vertical-drag nhiễu góc-cam). 3 tool site (water/ground/mound) gom `SiteDragTool` interface → dispatch loop (né complexity>10).
  **GOTCHA G0/grid:** `heightAt` BIAS FBM `[-1,1]→[0,1]` (`fbm·0.5+0.5`) = heightmap KHÔNG-âm + `Math.max(dy,0)` →
  Y = topY+Δy LUÔN ≥ topY (groundThick 10mm). FBM dao động ÂM → nền xấn dưới grid → **lỗ đen lộ void** (đã vấp).
  **PERF kéo slider:** terrain chỉ đụng ground geo → live-drag dùng **`_applyTerrainLive` SWAP geometry-only**
  (`groundGeometry()` export + `SiteHandle.ground` ref) — KHÔNG `_rebuildSite` (tránh tái-tạo water reflector RTT
  + recompile NodeMaterial = tụt fps). Buông = `applySite(true)` commit. Nặng nữa → hạ slider Resolution.
- **Form** zone/cut: `Rect | Tròn | Ellipse | Free (bezier)` (`groundFormRow`). Đổi → Free: seed blob 8-điểm
  (`rectBezierPoints`, DÙNG CHUNG với hồ) từ `length×width`. **Nắn 3D** (`interaction/groundDrag.ts` GroundTool, mirror
  waterDrag): Move + layer free active → overlay đỉnh (vàng) + 2 tay-cầm bezier in/out (cyan) + line; kéo đỉnh/tay-cầm
  → ghi `points[]` live + viền vàng định-vị; buông = commit (rebuild đặt lỗ-cut). Body-drag (dời layer) vẫn ở Lab `_layerDrag`.

## 3. Tầng & toạ độ (Y stacking)

```
baseY(lv=1) = groundThick/1000           # mặt trên base slab (mặc định 10mm = 0.01m)
zone (add): đáy=baseY, đỉnh=baseY + thickness/1000      # dày slider 1–10cm
top(level)  = baseY + max(thickness zones)/1000
cut: khoét XUYÊN TRỌN zone (difference); highlight phẳng @ y = top + 0.005 (nổi 5mm, KHÔNG khối)
baseY(lv+1) = top                         # level kế chồng tiếp lên
```

Local→world: `shapeToLocalPolygon` (mét, tâm gốc) + `offsetX/Z÷1000`. Shape XY dùng `(x, −z)` rồi `rotateX(−90)`.

## 4. Lỗi thường gặp

| Triệu chứng | Nguyên nhân (1 dòng) | Chi tiết & fix |
| --- | --- | --- |
| Cut để viền/góc thừa, wedge tròn/ellipse, cut==zone không xóa | earcut-hole chạm biên + cover-test ray-cast ở biên | `known-issues/KI-011` |
| Đáy/lớp dưới vô hình sau khoét | merge index lệch / backdrop đặc che | `known-issues/KI-004` |
| Kéo 1 layer → rebuild cả site | thiếu dirty-check / RTT tái tạo | `known-issues/KI-005` |

## 5. Lịch sử nâng cấp

- `2026-06-05` — site-kit GROUND đa-tầng `groundLayers[]` (box độc lập, xếp chồng Y, đục lỗ nước) + G1+ kéo-được
- `2026-06-06` — free-form shape (rect/circle/ellipse) + cut-reveal (clip-hole) + "bộ nền" phím tắt (Phase 2 inc 1)
- `2026-06-07` — nested-tabs G-level `[Mảng add|Khoét cut]` + cut highlight xám-khi-chọn + **khoét = polygon-boolean** (hết viền/góc/wedge, KI-011)
- `2026-06-07` — +4 texture surface (artificial_turf/grass_o/thai sand 2K+4K) → 3 hệ: ground surface + bộ nền (phím 5-7, `_photoEditorGround` share PhotoGround) + **water bottom** (texture đáy hồ, `basinMaterial` inject `groundMatByKey`); 4K né prefetch (chỉ tải khi chọn)
- `2026-06-07` — **add/cut mảng FORM TỰ DO** (Free bezier): `groundFormRow`+`Free`, seed blob `rectBezierPoints` (chung hồ), **GroundTool** (`interaction/groundDrag.ts`) nắn đỉnh+tay-cầm 3D (mirror waterDrag; outline-khi-kéo, commit-khi-buông; body-drag giữ Lab). Render contour cong đã sẵn từ `2026-06-06`
- `2026-06-07` — **+cobblestone** vào ground surface (GroundMaterialKey + GROUND_TEX_KEYS + GROUND_PRESETS #626263 + GROUND_TEX_SPEC + GROUND_OPTS) — nhân bản pattern texture (PhotoGround world-XZ, ktx2). Đá cobblestone lát sân/lối.
- `2026-06-07` — **G rỗng** (`site.groundLevels` count tường minh): `＋`G = +1 tầng RỖNG (bỏ auto-Z1) + **`✕ Remove G`** (xoá tầng + dồn level + clear active); editor enumerate `groundEditorLevels(1..N)`, render KHÔNG đổi (derive layers). Parse migrate `?? max(level)`, không bump schema
- `2026-06-07` — **TERRAIN Phase 1** (nền lồi-lõm sân vườn): `terrain.ts` (height-field 1-điểm-lan-truyền: value-noise FBM + domain-warp + Σ gò + mask rect, PURE) + `site.terrain` (TerrainConfig optional + parseTerrain, backward-compat) + `buildGround` lưới displaced (`griddedGroundGeometry`) + `opts.buildingFootprint` (mask pad nhà) + GUI section "🏔️ Terrain" tab Ground (10 slider: amplitude/frequency/octaves/lacunarity/gain/warp/seed/resolution/padMargin/edgeFlat, live-rebuild). mask=0 ở pad/hồ/viền → móng/hồ/rào không đổi. Phase 2 = cỏ bám gò; Phase 3 = nặn gò tay; Phase 4 = detail-normal. **Gaea = defer** (sai scale + phá live-edit; `heightAt` chừa cửa cộng heightmapSample). Mặc định TẮT (opt-in)
- `2026-06-07` — **Khoét lỗ hồ trên gò = THỐNG NHẤT** (xem bảng §2 "Khoét lỗ hồ"): (a) đổi nguồn lỗ gò `allWaterCarvePolygons`→**`waterPolygons`** = KHỚP path phẳng/editor (hết thủng-dưới-coping + puddle-lơ-lửng); (b) thay bỏ-nguyên-ô (răng cưa) bằng **CLIP ô-biên `polygon-clipping` + `ShapeUtils.triangulateShape`** (mép sạch như cut C1, `groundGeometry(.., clean)`); (c) `+heightAt` clamp `Math.max(dy,0)` né xấn-grid + G0-grid-floor. 1 nguồn duy nhất = `pondWorldXZ`
- `2026-06-08` — **TERRAIN: DRAPE = "LOANG LỔ" (cát phủ lổ-đổ lên cỏ) — biến xuyên-G0 thành tính năng**: xuyên-G0 (grid-mismatch) không trị tuyệt-đối được rẻ → NgQuan chốt biến thành feature. Drape ON + gò-riêng → zone (cát) **dao động QUANH** mặt G0 (cỏ) theo gò CENTERED (`heightAt(zhf) − ½amplitude` → ±amp/2): chỗ chìm dưới G0 = cỏ ló, chỗ nổi = cát → patches hữu cơ (amplitude=độ-tương-phản, frequency=cỡ-mảng). `zoneSurfaces` trả +`loangLo`; drape→loangLo=true (yAt=surface trực tiếp, KHÔNG taper/min-gap, interpenetrate=hiệu ứng; không gò-riêng→+GAP clean). **Drape OFF = đè trực tiếp** (loangLo=false: gò trên nền-phẳng + taper mép + min-gap, clean pad). `zoneYAtFn` tách (complexity). z-fight chỉ ở đường-giao (mảnh, chấp nhận). Thuần fromState.
- `2026-06-08` — **TERRAIN fix: zone drape HẾT XUYÊN G0 (g0-mesh-height, không true-curve)**: zone bám-G0 + gò-riêng-thấp → mép xuyên G0 vì `below` lấy `heightAt(G0)` (đường-cong THẬT) nhưng mặt G0 render là LƯỚI TAM-GIÁC (facet) → lệch ở giữa ô (>2cm GAP khi gò-zone thấp, ôm sát G0). Fix: **`g0MeshHeightAt`** — nội-suy TAM-GIÁC trên lưới G0 (khớp `pushGridQuad`: T1(00,01,10) khi fx+fz≤1 / T2(11,10,01)) → `below` = mặt G0 MESH ĐÚNG ở đỉnh zone ⇒ zone luôn = G0+GAP (hết xuyên). `DrapeCtx` +hw/hd/res (lưới G0). Residual giữa-đỉnh (lưới zone≠G0 chưa align) = bậc-2 nhỏ, GAP 2cm trùm. Thuần fromState.
- `2026-06-08` — **TERRAIN fix: G1 Y LUÔN > G0 Y (≠ bằng) + PERF zone-slider**: (1) **min-gap**: `ZONE_MIN_GAP=0.02` (2cm) — mép taper rủ về `below + GAP` (KHÔNG tới below) + `top = below + GAP + gò` → Y zone LUÔN ≥ G-dưới + 2cm ⇒ **không bao giờ bằng Y G0** theo chiếu-đứng (né z-fight + sai-số nội-suy lưới; gò dốc cực đoan có thể cần grid-align — defer). (2) **perf**: kéo slider terrain-zone xưa = `applySiteLive`→`_rebuildSite` tái-tạo **water reflector RTT mỗi frame** = tụt fps. Fix: zone slider → **`applyTerrainLive`** (mở rộng): swap base G0 + **`_rebuildGroundLayersLive`** (gỡ+dispose mesh zone cũ `userData.groundLayerIdx` + `buildGroundLayers(clean=false)` cell-drop) — KHÔNG đụng nước/cỏ/material-cache. `drapedLayerGeometry` +`clean`: commit=clip ô-biên sạch / live=bỏ-ô tâm-trong (`emitZoneQuad`, né clip Martinez). `buildGroundLayers` EXPORT. cut-patch own-mat dispose tránh leak. Buông → `applySite` clip sạch.
- `2026-06-08` — **TERRAIN fix: mép zone displaced HẠ THẤP TỰ NHIÊN (hết lơ lửng)**: zone bật gò/drape là mặt-đơn → mép hở, lơ lửng trên G dưới. Fix = **edge taper** (KHÔNG váy vuông góc): `zoneSurfaces` trả `{top, below}` (below = mặt-G-ngay-dưới = baseY+gò-G0, KHÔNG gò-zone/5mm); `drapedLayerGeometry` `yAt` = `below + smoothstep(distToPolyEdge/band)·(top−below)` → ở viền (dist→0) yAt=below (chạm G dưới), trong lòng = top đầy. band = `layer.terrain.edgeFlat` (cap ≤45% cạnh nhỏ). `distToPolyEdge`/`distPointSeg` (điểm→cạnh contour). Mép rủ MƯỢT xuống chạm mặt dưới theo chiếu-đứng. Thuần fromState. (Holes/cut chưa taper — chỉ outer contour; refine sau nếu cần.)
- `2026-06-08` — **TERRAIN: GÒ RIÊNG mỗi zone (G1/G2/z1/z2) — per-zone terrain**: `GroundLayer.terrain?: TerrainConfig` (parse, backward-compat undefined) → mỗi add-zone có noise + mounds RIÊNG (độc lập G0). Geometry: refactor `GridBuild`→`GridEmit` dùng hàm **`yAt(x,z)`** (thay hf+topY cứng) → `emitWorldTri`/grid dùng chung. **`zoneYAt(layer,baseY,drape)`** tổ hợp: `baseY+5mm + (gò G0 nếu drape) + (gò riêng zone nếu terrain.enabled)`; gò-zone = `makeHeightField(layer.terrain, [], 1e6,1e6)` (mask off → fill cả zone, clip contour ở geometry). `zoneCell` = cell G0 (drape) / zone-size÷res (gò riêng). `addZoneMesh` dùng `zoneYAt` (null→slab phẳng). Hoạt động CẢ khi G0 terrain TẮT (zone tự gò). **GUI:** parameterize `buildTerrainControls(body,ctx,t,applyLive,refresh,showDetail)` + `buildMoundControls`/`buildMoundRow` (+applyLive) → reuse cho zone trong `buildAddZoneExtras` (live=`applySiteLive`, showDetail=false). **Trực giao Drape:** drape=bám-G0, zone.terrain=gò-riêng → cộng dồn. **Honest:** cỏ vẫn theo G0 (không theo gò-riêng-zone — niche); zone.terrain.detail (vùi-lấp) = Feature sau. mounds Pos theo world (lot center).
- `2026-06-08` — **TERRAIN: zone DRAPE (bám gò) + toggle Phẳng-pad|Drape per-zone**: `GroundLayer.drape?` (state, parse, backward-compat false). **drape=false (mặc định)** = slab phẳng + gò làm-phẳng-dưới (mask, "pad"/patio — fix trước). **drape=true** = zone UỐN theo gò: `drapedLayerGeometry` (lưới `gridCell` = cạnh-lô/res trên bbox zone → mỗi ô `polygonClipping.intersection(ô,contour) − holes` → `ShapeUtils.triangulateShape` gồm lỗ-trong-ô → `emitWorldTri` Y=baseY+5mm+heightAt) — mặt ĐƠN thin-sheet đậu trên gò (bỏ thickness, chỉ dùng stacking). Refactor `GridBuild`→`GridEmit` (emitWorldTri DÙNG CHUNG nền G0 + zone). `zoneRects` chỉ flatten drape=false (drape=true KHÔNG vào mask → gò giữ nhấp nhô để zone uốn). `addZoneMesh` rẽ nhánh + thread `DrapeCtx{hf,cell}` qua buildLevelZones/buildGroundLayers (hf dựng 1 lần). GUI toggle "Drape (bám gò)" per add-zone. **Honest:** mặt đơn (không thickness-edge khi drape); zone chỉ build lúc commit (terrain drag = base swap, zone re-drape khi buông). **Trade-off:** clip mọi ô zone lúc commit (zone lớn → chậm hơn chút, không per-frame).
- `2026-06-08` — **TERRAIN fix: gò KHÔNG cướp đất zones** (G1/G2/z1/z2): gò ngẫu nhiên ở G0 nhấp nhô DƯỚI zone (ExtrudeGeometry slab PHẲNG, Y cố định) → nổi/lún mép. Fix = thêm footprint zones vào **mask terrain** (như nhà/hồ/viền): `layerRect` (bbox `layerWorldPolygon`) + `zoneRects(site)` (mọi ADD-layer, cut bỏ) → `buildHeightField` (nền) + `buildSiteGrass` (cỏ KHỚP) push thêm; `grassBuildSig` +groundLayers (zone đổi → re-scatter). Gò giữ PHẲNG dưới zone → zone giữ đất. **Trade-off:** bbox axis-aligned (non-rect/xoay hơi rộng) + có halo padMargin quanh zone (như nhà). Thuần site-kit (fromState), KHÔNG đụng Lab. Đóng nốt defer Phase-1 ("zone phẳng nổi trên gò").
- `2026-06-08` — **TERRAIN Phase 4** (detail micro-relief — HOÀN TẤT terrain): `PhotoGround` +option `detail`/`detailScale` + `setDetail` (live uniform `uDetail`) — perturbation normal procedural (`mx_noise_float` finite-diff 3-mẫu world-XZ × freq) **CỘNG lên** normal map (không thay) → phá vẻ "nhựa" gò nhẵn. `_normalNode` rẽ map-có/không (vắng → base +Y); `_detailNormal` tilt worldN.x/z theo gradient. Driver = `terrain.detail` (field cũ, đang chết): site-kit `groundMaterial` bơm `detail`; Lab `_groundMatFor` bơm + **`_applyTerrainDetail`** đẩy uniform vào MỌI PhotoGround cache (vượt ranh per-key-cache, KHÔNG recompile). GUI slider "Detail (sần)" — live=`applyTerrainDetail` (uniform, KHÁC slider noise=geometry), buông=`applySite`. **Honest:** CHỈ PhotoGround (ground texture); GrassGround/màu-phẳng không. uDetail=0 → no-op (vẫn tính noise ~0.1ms, negligible vs reflector). Phá vẻ nhựa rõ khi cận cảnh. meta 1.0→1.1. Gate tsc/eslint/check-perf/Vite xanh. **🏔️ Terrain Phase 1–4 XONG.**
- `2026-06-08` — **TERRAIN Phase 3b** (nặn gò 3D viewport): `archplan/interaction/moundDrag.ts` **MoundTool** (mirror GroundTool/WaterTool) — mỗi mound 2 handle: TÂM (sphere vàng, kéo XZ-plane → `x/z`) + BÁN KÍNH (sphere cyan mép +X, kéo → `radius` = dist tâm→con-trỏ, clamp khớp parseMounds). Kéo = `applyTerrainLive` (swap geo nền, rẻ) · buông = commit; đĩa footprint mờ (`depthTest:false`) lúc kéo. Handle `depthTest:false`+renderOrder cao → luôn grab dù gò che. Chỉ hiện khi terrain bật + moveMode. Height/Falloff giữ slider (vertical-drag phụ-thuộc-góc-cam, slider chính xác hơn). Wire ArchPlanLab: pointer down/move/up + setMoveMode + rebuildSite + dispose + `_moundHost`. **Refactor complexity:** 3 tool site (💧water/🟫ground/⛰️mound) giờ ≥3 → gom **`SiteDragTool` interface** + `_siteDragTools()` + dispatch LOOP (`_tryStartSiteTool`/`_dragSiteTool`/cancel/rebuild/dispose) thay 3 method chạm trần-10 (mỗi `?.tool` +1). Gotcha TS: type-guard narrow về union CLASS không phải interface (class có private → predicate sai chiều). Commit archplan kế. Gate tsc/eslint(complexity OK)/check-perf/Vite xanh
- `2026-06-08` — **TERRAIN Phase 3a** (nặn gò tay — GUI): `archplan/gui/site.ts` `buildMoundControls`+`buildMoundRow`+`moundSliderSpecs` — list mounds + `＋Thêm gò`(default tâm-lô R2m H40cm, tự bật terrain)/`✕Xoá` + 5 slider Pos X/Z·Radius·Height·Falloff (STRUCTURAL → `applyTerrainLive` kéo / `applySite` buông như slider noise; thêm/xoá → `refresh()`). Ghi `terrain.mounds[]` → `heightAt` cộng `moundsHeightMm` (Σ gò, đã có từ P1) → griddedGroundGeometry sample. KHÔNG đụng lõi (state.ts/terrain.ts mounds sẵn). height ÂM = lõm bị grid-floor chặn (cancel noise, không thủng base). `parseMounds` clamp bao trọn range GUI. Nút inline-style (né `archplan-lab.css` = Factory roof). Phase 3b = nặn 3D viewport (click-drag mirror waterDrag)
- `2026-06-07` — **TERRAIN Phase 2** (cỏ bám gò): `GrassBlades` +option `heightAt?: (x,z)=>number` → `_scatter` nâng gốc lá `gy = baseY + heightAt` + `_emitContacts` vệt tiếp đất nâng theo (sample TÂM cụm). `buildSiteGrass` dựng `makeHeightField(terrain, exclude, ...)` — **`exclude` (foundation+hồ) ≡ maskRects nền** (cùng `_foundationRects`+`waterRect`) ⇒ cỏ ngồi KHÍT mặt nền displaced; bơm closure `heightAt: hf?(x,z)=>heightAt(hf,x,z):undefined`. `grassBuildSig` +terrain → đổi terrain RE-SCATTER (chỉ lúc commit/buông, KHÔNG per-frame: terrain drag = `_applyTerrainLive` ground-only, cỏ giữ tới buông). 0 cost runtime (sample build-time). Module vẫn ĐỘC LẬP (caller bơm closure). Phase 3 = nặn gò tay; Phase 4 = detail-normal
- `2026-06-11` — **MIX STAGE 4: lên MẶT ĐỨNG — fence + vách hồ + đáy hồ** (NgQuan "bỏ nền mix vào tất cả tường fence, thành hồ bơi wall water, bottom water"): `PhotoGroundMix` +option **`mapping: 'xz'|'uv'|'wall'`** — `'xz'` world-XZ như cũ (nền/zone/**đáy hồ** — đáy nằm nên dùng NGUYÊN XI, cọ vẽ world-rect bbox hồ); `'uv'` đọc **attribute uv MÉT** baked (vách hồ `basinWallsGeometry` chu-vi×cao — cọ vẽ qua `isect.uv`, rect uniform = (0, yBot, perim, h)); `'wall'` planar đứng tự chọn trục theo |normal| (tường rào boxes/stone KHÔNG có uv thật — **KHÔNG cọ vẽ** v1, vẽ sẽ mirror 2 cạnh đối → cần bake uv chu-vi stage sau). Normal frame mapping≠'xz' = **TBN tại-mặt** (T=cross(up,N)+eps né suy biến đỉnh tường, B≈+Y). **State:** `WaterConfig.floorMix/wallMix` + `FenceConfig.mix` (+`optMix` parse, backward-compat). **Render:** `opts.waterMixMat(w,face)`/`fenceMixMat(f)` — mix THẮNG material đơn, null → fallback; `addBasinMesh` tag `userData.waterMixRef/waterMixFace` (stamp raycast). **Lab:** cache `_zoneMix` RE-KEY từ target → **chính object `GroundMixParams`** (target giờ nhân ra 5 loại, wrapper không stable; params object sống tại chỗ trong state); `_mixSpaceOf`/`_mixWaterRect`/`_mixMeshMatch`/`_mixHitOf`; `_pruneZoneMix` gom `_liveMixParams` MỌI nguồn; `_ensureGroundTex` xong +`_fenceSig=''` (fence mix hết kẹt null chờ texture). **GUI:** `MixPaintTarget` +`{water,face}`/`{fence}` + **`sameMixTarget`** (wrapper so theo ref config, KHÔNG ===); `mkMixSection` generic (toggle+board) cắm vào Water▸Bottom▸Floor/Walls + Fence pane; `buildMixBoard` +`paintable` (fence ẩn hàng cọ); `ensureMixCss` NỚI scope `.ap-scan-panel.ap-site-panel` (board sống ngoài ground-domain; riêng bậc màu tab Z giữ `.ap-ground-domain`). **Building wall = deferred** (`deferred/systems/building-wall-mix.md` — building/* đang active luồng song song). Móng cho **mix tường rêu phong 3 rule trọng lực** (bước kế). Gate tsc 0 + eslint 0 cả 2 repo.
- `2026-06-11` — **TƯỜNG RÊU PHONG (3 rule trọng lực) + cinder/stone NHÓM CHUNG KHO NỀN** (NgQuan: "mix vào thì cinder/stone của fence mất → nhóm chung vào bộ texture nền luôn, sau đó làm tường rêu phong"): (1) **Kho nền +2 key tường** `'cinder-blocks-wall'` + `'stone-wall'` (GroundMaterialKey + GROUND_TEX_KEYS + GROUND_PRESETS + GROUND_TEX_SPEC Lab — TÁI DÙNG url imports `_ensureFenceTex` sẵn có, HTTP cache không tải đôi + GROUND_OPTS GUI) → chọn được ở MỌI nơi: mix board (MIX_TEX_OPTS tự ăn theo isGroundTexKey), ground select, defBase fence='cinder-blocks-wall'/vách hồ='stone-wall'. (2) **3 RULE TRỌNG LỰC kiểu Substance generators** — `GroundMixSlot.rule?: 'foot'|'streak'|'moss'` (bake vào graph — vào SIG cache cùng keys texture; đổi rule = structural) + `GroundMixParams.gravity` (cường độ 0..1, uniform LIVE): `_ruleMask` trong PhotoGroundMix — **'foot'** chân bùn = 1−smoothstep(8..60% cao) · **'streak'** vệt nước chảy dọc = fbm NÉN trục y (3.1:0.22) sharpen 0.52..0.78 × đậm dần xuống · **'moss'** rêu ẩm = gradient sát chân (0..45%) × fbm mép nham nhở; cộng `(rule−0.5)·2·gravity` vào raw TRƯỚC smoothstep (gravity=0 = thuần fbm). Trục y qua **`setWallRange(footY, h)`** (uniform live — Lab `_applyMixWallRange`: vách hồ = (rim−depth, depth), rào = (mặt nền, height)); CHỈ ăn mapping≠'xz'. **GUI:** slot mặt đứng (isVerticalMixTarget) +select "Quy luật" (mkSlotRuleRow) + board +slider "Trọng lực". "Rêu hướng ít nắng" = deferred (cần sun-dir uniform). Gate tsc 0 + eslint 0 cả 2 repo.
- `2026-06-11` — **UI nền mix GỌN LẠI** (NgQuan, polish bảng trộn trong hệ Ground — KHÔNG đụng Lab float `ground-lab.ts`): `ensureMixCss()` (inject `<style id=ap-mix-css>` scoped `.ap-ground-domain`, palette `--gr-*` — pattern như `ensureCutGrayCss`, KHÔNG sửa `archplan-lab.css`). (1) **select gọn** (`.ap-mix-sel` nâu đất, flex:1). (2) **bỏ button 🖌 + ✕ per-slot**: xóa lớp = option `✕ Xóa lớp` (sentinel `__del`) TRONG select; chọn lớp vẽ = **click slot row** (tag số + viền-trái accent `.ap-mix-active`); vẽ = **1 nút 🖌 CHUNG** ở hàng cọ (`mkMixBrushRows` trả `{rows, sync}`; `activeSlot` = GUI-state closure sống qua redraw). (3) **khu slot = block tách** (`.ap-mix-slots` viền/nền nhẹ) khỏi 6 slider chung (tách `mkMixSliders`). (4) nút "Xóa nét" → **symbol ↺** + **`confirmPopup`** (export từ `roof-lab.ts`, tái dùng `ap-roof-confirm`) NGANG hàng ô Tẩy. (5) **Drape (bám gò) đẩy LÊN** hàng đôi cạnh "Mix nền" (`buildSurfaceZoneBody`; gỡ khỏi `buildAddZoneExtras`). (6) **divider** `.ap-terrain-hdr` (border-top) tách khu mix ↔ terrain gò. (7) **bỏ bảng Lot/House/Coverage/Garden** (`readout()` xóa; `ctx.siteStats`/`registerSiteReadout` giữ trong interface — optional-chain vô hại). (8) **khu tab Z/P/B/W tối 1 nấc** (`ap-zone-itabs` chỉ op add → `var(--gr-bg-2)`, lồng cấp như Garden; cut giữ xám riêng). Gate tsc 0 + eslint 0 (archplan + modules).

## 6. Liên hệ

- **Modules:** [fromState.ts](../threejs-modules/site/render/fromState.ts) · [state.ts](../threejs-modules/site/state.ts) · [shapes.ts](../threejs-modules/site/shapes.ts) (tessellate rect/circle/ellipse/free) · [terrain.ts](../threejs-modules/site/terrain.ts) (height-field gò: noise FBM + mask + Σ gò)
- **Editor:** archplan `gui/site.ts` (nested-tabs + Form + cut highlight) · `interaction/groundDrag.ts` (GroundTool nắn đỉnh/tay-cầm free) · `ArchPlanLab.ts` (`_applyCutVisibility` + `_activeLayerIdx` + `_layerDrag` body)
- **Deferred:** WYSIWYG live-rebuild geometry khi kéo (hiện chỉ outline) · cut "lộ lớp riêng từng level" (hiện lộ-xuyên theo stacking) · offsetPolygon concave gắt tự-cắt (lib offset robust) · **Terrain P2-4** (cỏ bám gò / nặn gò tay 3D / detail-normal) · **Gaea heightmap import** ([deferred/systems/terrain-gaea-heightmap.md](../deferred/systems/terrain-gaea-heightmap.md)) · zone/layer phẳng nổi trên gò (san phẳng cục bộ) · terrain ground skirt (hiện mặt-trên-đơn)
- **KI:** KI-011 (cần polygon-boolean) · KI-004 · KI-005
